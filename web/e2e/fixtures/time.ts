/** Frozen "now" for all e2e (MSK). Horizon: 2026-03-12 … 2026-03-25. */
export const FROZEN_NOW_ISO = '2026-03-11T12:00:00+03:00';

/** Default open bookable date used in happy-path flows. */
export const OPEN_DATE = '2026-03-13';

/** Fully closed date — must not appear in the date picker (UC-7). */
export const CLOSED_DATE = '2026-03-18';

/** Today under frozen clock (excluded from horizon). */
export const TODAY = '2026-03-11';
